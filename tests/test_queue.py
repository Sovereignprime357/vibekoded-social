# RETIRED — DO NOT USE. Renamed to test_content_queue.py.
#
# This file used to test the old queue.py module. That module was renamed
# to content_queue.py (see the note at the top of ../queue.py for why), and
# its tests moved to test_content_queue.py accordingly.
#
# This file is intentionally left as an empty stub (rather than deleted)
# because the sandbox this was built in cannot unlink files on this mount.
# SAFE TO DELETE BY HAND: `rm tests/test_queue.py` (or delete via Explorer)
# on the host filesystem — pytest collects it but it contains no tests, so
# it is harmless to leave in place; deleting it is just cleanup.
